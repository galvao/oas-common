# oas-common
Common [OAS](https://www.openapis.org) HTTP components written in YAML to be reused in API Specifications via the `$ref` attribute.

The motivation is to keep from re-writing specification code, since some situations are, in the opinion of the author, universal.
